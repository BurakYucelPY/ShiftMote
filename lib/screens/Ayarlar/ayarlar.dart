import 'package:flutter/material.dart';

import 'ayarlar_view.dart';

class Ayarlar extends StatelessWidget {
  const Ayarlar({super.key});

  @override
  Widget build(BuildContext context) => const AyarlarView();
}
